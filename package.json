{
  "name": "atoz-links-root",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd frontend && npm run dev -- --port=3000 --host=0.0.0.0",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview -- --port=3000 --host=0.0.0.0",
    "start": "cd frontend && npm run preview -- --port=3000 --host=0.0.0.0",
    "lint": "cd frontend && npm run lint"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
