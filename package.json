{
  "name": "atoz-links-root",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend -- --port=3000 --host=0.0.0.0",
    "build": "npm run build -w frontend",
    "preview": "npm run preview -w frontend -- --port=3000 --host=0.0.0.0",
    "start": "npm run preview -w frontend -- --port=3000 --host=0.0.0.0",
    "lint": "npm run lint -w frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
